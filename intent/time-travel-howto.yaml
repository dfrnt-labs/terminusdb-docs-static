# Intent Annotations: time-travel-howto
# Page: src/app/docs/time-travel-howto/page.md
# Title: How to Time-Travel (Query Historical Data)
# Total blocks: ~20 (estimated)
# Annotated by: systems-engineer (Phase III)
# Date: 2026-05-02
# Fixture: Requires TerminusDB running with admin/mydb and some commit history

blocks:
  # Section 1: Get commit history
  - block_index: 0
    language: bash
    action: "Get commit log via GET /api/log"
    expected_outcome:
      type: count_gte
      value: "Returns array of commits with identifier, author, message, timestamp"
      tolerance: count_gte
    depends_on: null
    skip_reason: null
    assertion: "Exit code 0 AND stdout is JSON array AND array length >= 1 AND each entry has 'identifier' and 'author' fields."

  - block_index: 1
    language: json
    action: "Display expected commit log response"
    expected_outcome:
      type: display_only
      value: "3 commits with identifiers, authors, messages, timestamps"
      tolerance: non_empty
    depends_on: 0
    skip_reason: display_only
    assertion: "No assertion — display only (illustrative SHAs)."

  - block_index: 2
    language: typescript
    action: "Get commit log via TypeScript SDK"
    expected_outcome:
      type: display_only
      value: "SDK log retrieval example"
      tolerance: non_empty
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — illustrative SDK code."

  - block_index: 3
    language: python
    action: "Get commit log via Python SDK"
    expected_outcome:
      type: display_only
      value: "SDK log retrieval example"
      tolerance: non_empty
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — illustrative SDK code."

  # Section 2: Query at a specific commit
  - block_index: 4
    language: bash
    action: "GET documents at a specific commit SHA"
    expected_outcome:
      type: response_body
      value: "Returns document state as it was at that commit"
      tolerance: non_empty
    depends_on: 0
    skip_reason: null
    assertion: "Exit code 0 AND stdout is valid JSON AND contains document data. NOTE: requires actual SHA from block 0 output."

  - block_index: 5
    language: json
    action: "Display expected time-travel query result"
    expected_outcome:
      type: display_only
      value: "Document with historical field values"
      tolerance: non_empty
    depends_on: 4
    skip_reason: display_only
    assertion: "No assertion — display only."

  - block_index: 6
    language: typescript
    action: "Query at commit via TypeScript SDK"
    expected_outcome:
      type: display_only
      value: "SDK time-travel query example"
      tolerance: non_empty
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — illustrative SDK code."

  - block_index: 7
    language: python
    action: "Query at commit via Python SDK"
    expected_outcome:
      type: display_only
      value: "SDK time-travel query example"
      tolerance: non_empty
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — illustrative SDK code."

  # Section 3: Compare points in time (diff)
  - block_index: 8
    language: bash
    action: "Diff two commits to see structural changes over time"
    expected_outcome:
      type: output_contains
      value: "SwapValue or Insert operations in response"
      tolerance: contains
    depends_on: 0
    skip_reason: null
    assertion: "Exit code 0 AND stdout contains '@op' or '@insert'. NOTE: requires SHAs from block 0."

  - block_index: 9
    language: json
    action: "Display expected diff between commits"
    expected_outcome:
      type: display_only
      value: "SwapValue operations showing what changed between commits"
      tolerance: non_empty
    depends_on: 8
    skip_reason: display_only
    assertion: "No assertion — display only."

  - block_index: 10
    language: typescript
    action: "Diff commits via TypeScript SDK"
    expected_outcome:
      type: display_only
      value: "SDK commit diff example"
      tolerance: non_empty
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — illustrative SDK code."

  - block_index: 11
    language: python
    action: "Diff commits via Python SDK"
    expected_outcome:
      type: display_only
      value: "SDK commit diff example"
      tolerance: non_empty
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — illustrative SDK code."
