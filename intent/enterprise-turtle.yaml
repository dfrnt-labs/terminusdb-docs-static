# Intent Capture: enterprise-turtle
# Page: src/app/docs/enterprise-turtle/page.md
# Title: Turtle Support
# Total blocks: 5
# Annotated by: systems-engineer (technical assertions)
# Date: 2026-05-02
# Status: COMPLETE
# Note: Enterprise reference page for Turtle (TTL) format support. One curl GET, expected Turtle output, subdocument nesting examples, and one curl POST (echo+pipe pattern). Requires TerminusDB Enterprise with existing Product data.

page: enterprise-turtle
total_blocks: 5
auto_filled: 5
needs_manual: 0
auto_fill_rate: 100%

blocks:

  - block_index: 0
    language: bash
    line: 28
    type: fenced
    action: "GET /api/document with type=Product&format=turtle — retrieve products serialized as Turtle"
    expected_outcome:
      type: status_code
      value: "HTTP 200 — returns valid Turtle with @base, @prefix declarations and typed literals"
      tolerance: exact
    depends_on: null
    skip_reason: null
    note: "Requires $AUTH, $SERVER, $DB variables set and Product documents in database. Enterprise feature."
    assertion: "Exit code 0 AND HTTP 200 AND response contains '@prefix' AND 'schema:Product'. SKIP if no Product data or non-Enterprise."

  - block_index: 1
    language: turtle
    line: 34
    type: fenced
    action: "Display expected Turtle output: Product/SKU-1001 with @base, @prefix, typed literals, and subdocument blank node"
    expected_outcome:
      type: skip
      value: "Shows full Turtle: @base, @prefix declarations for schema/xsd/rdf, Product with 'a schema:Product', ^^xsd:decimal prices, blank node [...] for UnitOfMeasure subdocument."
      tolerance: null
    depends_on: 0
    skip_reason: no_test_runner
    assertion: "No assertion — no Turtle test runner. Expected output reference."

  - block_index: 2
    language: turtle
    line: 71
    type: fenced
    action: "Display Turtle document reference syntax: full IRI in angle brackets for cross-document links"
    expected_outcome:
      type: skip
      value: "Shows schema:category <terminusdb:///data/Category/ELEC> — full IRI reference to another document."
      tolerance: null
    depends_on: null
    skip_reason: no_test_runner
    assertion: "No assertion — display only. Turtle reference syntax example."

  - block_index: 3
    language: turtle
    line: 79
    type: fenced
    action: "Display Turtle subdocument nesting: blank node [...] syntax for @subdocument classes"
    expected_outcome:
      type: skip
      value: "Shows schema:unit [ a schema:UnitOfMeasure ; schema:symbol 'pcs' ; schema:name 'pieces' ] — nested blank node for subdocument."
      tolerance: null
    depends_on: null
    skip_reason: no_test_runner
    assertion: "No assertion — display only. Turtle blank node nesting for subdocuments."

  - block_index: 4
    language: bash
    line: 93
    type: fenced
    action: "POST Turtle document via echo pipe + --data-binary @- (Content-Type: text/turtle)"
    expected_outcome:
      type: status_code
      value: "HTTP 200 — document created from Turtle input"
      tolerance: exact
    depends_on: 0
    skip_reason: null
    note: "Uses echo|curl pattern because Turtle starts with @ which curl interprets as filename. --data-binary preserves newlines. Requires schema with Product/Category/UnitOfMeasure classes."
    assertion: "Exit code 0 AND HTTP 200 AND response contains 'Product/SKU-3001'. SKIP if schema not set up."
