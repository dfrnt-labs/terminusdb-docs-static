# Intent: access-control-tutorial
# Page: src/app/docs/access-control-tutorial/page.md
# Title: Access Control Tutorial
# Generated: 2026-05-01 (skeleton), 2026-05-02 (UX annotations by design-engineer)
# Status: UX annotations complete
#
# UX annotations by: design-engineer
# Note: This is a TUTORIAL page — step-by-step learning journey for access control.
#       Reader arrives wanting to: set up a second user with restricted access.
#       Outcome: understand users, roles, capabilities, and permission enforcement.

page: access-control-tutorial
total_blocks: 12
auto_filled: 12
needs_manual: 0
auto_fill_rate: 100%

blocks:

  # === Step 1: Create database + insert document ===

  - block_index: 0
    language: bash
    line: 36
    type: fenced
    action: "Create a database and insert a test document (two curl commands)"
    expected_outcome:
      type: status_code
      value: 200
      tolerance: exact
    depends_on: null
    skip_reason: null
    ux_intent: "Reader learns: first create a database (POST /api/db/admin/MyDatabase), then insert a raw JSON document (POST /api/document with raw_json=true). The document has a custom @id. Two commands establish the data we will protect with access control."

  - block_index: 1
    language: bash
    line: 51
    type: fenced
    action: "Verify the inserted document is accessible (GET by ID)"
    expected_outcome:
      type: status_code
      value: 200
      tolerance: exact
    depends_on: 0
    skip_reason: null
    ux_intent: "Reader learns: GET /api/document with id= param retrieves a specific document. Confirms data exists before we start restricting access. Establishes the baseline: admin can read this document."

  # === Step 2: Create user ===

  - block_index: 2
    language: bash
    line: 62
    type: fenced
    action: "Create a new user 'alice' with a password (POST /api/users)"
    expected_outcome:
      type: status_code
      value: 200
      tolerance: exact
    depends_on: 1
    skip_reason: null
    ux_intent: "Reader learns: POST /api/users with {name, password} creates a user. The user exists but has ZERO capabilities — cannot access any database until explicitly granted. Separation of identity from access."

  - block_index: 3
    language: json
    line: 69
    type: fenced
    action: "Display expected create-user response (user IRI)"
    expected_outcome:
      type: skip
      value: null
      tolerance: null
    depends_on: null
    skip_reason: display_only
    ux_intent: "Reader sees: response is a string IRI 'terminusdb://system/data/User/alice'. Confirms user was created. The IRI is the user's permanent system identifier."

  - block_index: 4
    language: bash
    line: 75
    type: fenced
    action: "Verify user alice exists (GET /api/users/alice)"
    expected_outcome:
      type: status_code
      value: 200
      tolerance: exact
    depends_on: 2
    skip_reason: null
    ux_intent: "Reader learns: GET /api/users/{username} verifies a user exists. Trust but verify — confirm creation before proceeding to grant access."

  # === Step 3: Grant read-only access ===

  - block_index: 5
    language: bash
    line: 83
    type: fenced
    action: "Grant Consumer Role (read-only) to alice on MyDatabase (POST /api/capabilities)"
    expected_outcome:
      type: status_code
      value: 200
      tolerance: exact
    depends_on: 3
    skip_reason: null
    ux_intent: "Reader learns: POST /api/capabilities with operation='grant', scope_type='database', scope='admin/MyDatabase', user='alice', roles=['Consumer Role']. This is the core RBAC operation — linking a user to a role on a resource. Consumer Role = class_frame + instance_read + schema_read. Alice can now read but not write."

  # === Step 4: Verify read works ===

  - block_index: 6
    language: bash
    line: 101
    type: fenced
    action: "Read document as alice (verify read access works)"
    expected_outcome:
      type: status_code
      value: 200
      tolerance: exact
    depends_on: 4
    skip_reason: null
    ux_intent: "Reader learns: authenticate as alice (-u alice:alice-secret) and GET the same document. Success = 200 with document JSON. Proves the grant worked — alice can read. The same URL that worked for admin now works for alice."

  # === Step 5: Verify write denied ===

  - block_index: 7
    language: bash
    line: 112
    type: fenced
    action: "Attempt to insert a document as alice (expect 403 Forbidden)"
    expected_outcome:
      type: status_code
      value: 200
      tolerance: exact
    depends_on: 5
    skip_reason: null
    ux_intent: "Reader learns: alice tries POST (insert) and gets 403 Forbidden. The access control system enforces role boundaries — Consumer Role has no write actions. This is the proof that RBAC is working: same user, same database, different operation = denied."

  - block_index: 8
    language: json
    line: 120
    type: fenced
    action: "Display expected 403 error response"
    expected_outcome:
      type: skip
      value: null
      tolerance: null
    depends_on: null
    skip_reason: display_only
    ux_intent: "Reader sees: {api:ErrorResponse, api:message: 'Insufficient capabilities for this operation', api:status: api:forbidden}. Clear error message — not a generic 403 but a specific capabilities failure. Reader knows exactly what the error looks like."

  # === Step 6: Upgrade to write (optional) ===

  - block_index: 9
    language: bash
    line: 134
    type: fenced
    action: "Create a custom writer role and grant it to alice (two curl commands)"
    expected_outcome:
      type: status_code
      value: 200
      tolerance: exact
    depends_on: 6
    skip_reason: null
    ux_intent: "Reader learns: (1) create custom role with POST /api/roles specifying name + action array (commit_write, instance_read/write, schema_read, class_frame), (2) grant it to alice on MyDatabase. Additive — alice now has BOTH Consumer Role and writer role. Demonstrates principle of least privilege: create exactly the actions needed."

  - block_index: 10
    language: bash
    line: 163
    type: fenced
    action: "Insert a document as alice (now succeeds with writer role)"
    expected_outcome:
      type: status_code
      value: 200
      tolerance: exact
    depends_on: 7
    skip_reason: null
    ux_intent: "Reader learns: same insert that failed in Step 5 now succeeds. The writer role grants instance_write_access + commit_write_access = ability to insert. Confirms the upgrade worked. The user-facing proof: alice can now add data."

  # === Step 7: Clean up ===

  - block_index: 11
    language: bash
    line: 174
    type: fenced
    action: "Revoke access and delete user alice (cleanup)"
    expected_outcome:
      type: status_code
      value: 200
      tolerance: exact
    depends_on: 8
    skip_reason: null
    ux_intent: "Reader learns: (1) revoke capability with operation='revoke' (mirror of grant), (2) DELETE /api/users/alice removes the user. Clean up after tutorial. Key insight: revoke is immediate — next request from alice would fail. Demonstrates reversibility of access control."
