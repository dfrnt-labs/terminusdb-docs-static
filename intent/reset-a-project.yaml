# Intent annotations for: reset-a-project
# Annotated: 2026-05-01
# Source: src/app/docs/reset-a-project/page.md
#
# This page teaches: get commit history, reset branch to a specific commit
# Prerequisites: TerminusDB running on localhost:6363, database with commit history
# Dataset: Self-created (admin/mydb with prior commits)

page: reset-a-project
total_blocks: 7
annotated: 7
needs_manual: 0

blocks:

  # Get commit history — TypeScript SDK
  - block_index: 0
    language: typescript
    line: 38
    type: fenced
    action: "Get commit history via TypeScript SDK client.getCommitHistory()"
    expected_outcome:
      type: non_empty
      value: "Array of commit objects with identifiers, messages, authors"
      tolerance: non_empty
    depends_on: null
    skip_reason: null

  # Get commit history — Python SDK
  - block_index: 1
    language: python
    line: 46
    type: fenced
    action: "Get commit history via Python SDK client.log(count=5)"
    expected_outcome:
      type: non_empty
      value: "Prints identifier, message, author for each commit"
      tolerance: non_empty
    depends_on: 0
    skip_reason: null

  # Get commit history — Example output display
  - block_index: 2
    language: json
    line: 54
    type: fenced
    action: "Shows expected commit log entry structure (InitialCommit with identifier, author, message, timestamp)"
    expected_outcome:
      type: skip
      value: null
      tolerance: null
    depends_on: null
    skip_reason: display_only

  # Get commit history — HTTP API
  - block_index: 3
    language: bash
    line: 66
    type: fenced
    action: "GET /api/log/admin/mydb/local/branch/main?count=5 — retrieve commit log via HTTP"
    expected_outcome:
      type: status_code
      value: "200"
      tolerance: exact
    depends_on: 2
    skip_reason: null

  # Reset branch — TypeScript SDK
  - block_index: 4
    language: typescript
    line: 79
    type: fenced
    action: "Reset branch to specific commit via TypeScript SDK client.resetBranch()"
    expected_outcome:
      type: non_empty
      value: "Branch HEAD moved to specified commit, subsequent commits discarded"
      tolerance: non_empty
    depends_on: 3
    skip_reason: null

  # Reset branch — Python SDK
  - block_index: 5
    language: python
    line: 88
    type: fenced
    action: "Reset branch to specific commit via Python SDK client.reset()"
    expected_outcome:
      type: non_empty
      value: "Branch HEAD moved to specified commit"
      tolerance: non_empty
    depends_on: 4
    skip_reason: null

  # Reset branch — HTTP API
  - block_index: 6
    language: bash
    line: 94
    type: fenced
    action: "POST /api/reset/admin/mydb/local/branch/main — reset branch HEAD to commit descriptor"
    expected_outcome:
      type: output_contains
      value: "api:success"
      tolerance: contains
    depends_on: 5
    skip_reason: null
