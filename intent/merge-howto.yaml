# Intent Annotations: merge-howto
# Page: src/app/docs/merge-howto/page.md
# Title: How to Merge Branches
# Total blocks: 25 (estimated)
# Annotated by: systems-engineer (Phase III)
# Date: 2026-05-02
# Fixture: Requires TerminusDB running with admin/mydb and branches main + feature

blocks:
  # Section 1: Merge a branch into main
  - block_index: 0
    language: bash
    action: "Merge feature into main via HTTP POST /api/apply"
    expected_outcome:
      type: output_contains
      value: "api:ApplyResponse with api:success"
      tolerance: contains
    depends_on: null
    skip_reason: null
    assertion: "Exit code 0 AND stdout contains 'api:success'"

  - block_index: 1
    language: json
    action: "Display expected merge success response"
    expected_outcome:
      type: display_only
      value: '{"@type": "api:ApplyResponse", "api:status": "api:success"}'
      tolerance: exact
    depends_on: 0
    skip_reason: display_only
    assertion: "No assertion — display only."

  - block_index: 2
    language: typescript
    action: "Merge via TypeScript SDK client.apply()"
    expected_outcome:
      type: display_only
      value: "SDK apply example"
      tolerance: non_empty
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — illustrative SDK code."

  - block_index: 3
    language: python
    action: "Merge via Python SDK client.apply()"
    expected_outcome:
      type: display_only
      value: "SDK apply example"
      tolerance: non_empty
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — illustrative SDK code."

  # Section 2: Detect conflicts
  - block_index: 4
    language: bash
    action: "Attempt merge that produces conflict"
    expected_outcome:
      type: output_contains
      value: "api:conflict"
      tolerance: contains
    depends_on: null
    skip_reason: null
    assertion: "Exit code 0 AND stdout contains 'api:conflict' AND stdout contains 'api:witnesses'"

  - block_index: 5
    language: json
    action: "Display expected conflict response with witnesses"
    expected_outcome:
      type: display_only
      value: "api:conflict with SwapValue showing conflicting values"
      tolerance: contains
    depends_on: 4
    skip_reason: display_only
    assertion: "No assertion — display only."

  # Section 3: Resolve conflicts
  - block_index: 6
    language: bash
    action: "Force update document to resolve conflict before re-merge"
    expected_outcome:
      type: output_contains
      value: "Document ID in response"
      tolerance: contains
    depends_on: 4
    skip_reason: null
    assertion: "Exit code 0 AND stdout contains document ID reference."

  - block_index: 7
    language: bash
    action: "Re-attempt merge after conflict resolution"
    expected_outcome:
      type: output_contains
      value: "api:success"
      tolerance: contains
    depends_on: 6
    skip_reason: null
    assertion: "Exit code 0 AND stdout contains 'api:success'"

  # Remaining blocks are SDK examples (display-only)
  - block_index: 8
    language: typescript
    action: "Conflict detection in TypeScript (try/catch)"
    expected_outcome:
      type: display_only
      value: "SDK conflict handling pattern"
      tolerance: non_empty
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — illustrative SDK code."

  - block_index: 9
    language: python
    action: "Conflict detection in Python (try/except)"
    expected_outcome:
      type: display_only
      value: "SDK conflict handling pattern"
      tolerance: non_empty
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — illustrative SDK code."
