# Intent Capture: delta-rollup
# Page: src/app/docs/delta-rollup/page.md
# Title: Perform a Delta Rollup
# Total blocks: 6
# Annotated by: systems-engineer (technical assertions)
# Date: 2026-05-02
# Status: COMPLETE
# Note: How-to for delta rollup/optimize API. 3 curl commands (branch, repo, system), 1 JSON response display, 2 CLI commands. All require running TerminusDB. Auto-optimize plugin handles this in standard deployments.

page: delta-rollup
total_blocks: 6
auto_filled: 6
needs_manual: 0
auto_fill_rate: 100%

blocks:

  - block_index: 0
    language: bash
    line: 62
    type: fenced
    action: "POST /api/optimize/admin/MyDatabase/local/branch/main — optimize a branch (most common)"
    expected_outcome:
      type: status_code
      value: "HTTP 200 with OptimizeResponse success"
      tolerance: exact
    depends_on: null
    skip_reason: null
    note: "Requires 'MyDatabase' to exist. Idempotent — safe to run on already-optimized DB. Uses admin:root basic auth."
    assertion: "Exit code 0 AND HTTP 200 AND response contains 'api:success'. SKIP if MyDatabase does not exist."

  - block_index: 1
    language: bash
    line: 71
    type: fenced
    action: "POST /api/optimize/admin/MyDatabase/local — optimize the repository layer (commit graph)"
    expected_outcome:
      type: status_code
      value: "HTTP 200 with OptimizeResponse success"
      tolerance: exact
    depends_on: null
    skip_reason: null
    note: "Optimizes commit graph layer, not instance data. Requires 'MyDatabase' to exist."
    assertion: "Exit code 0 AND HTTP 200 AND response contains 'api:success'. SKIP if MyDatabase does not exist."

  - block_index: 2
    language: bash
    line: 80
    type: fenced
    action: "POST /api/optimize/_system — optimize the system database"
    expected_outcome:
      type: status_code
      value: "HTTP 200 with OptimizeResponse success"
      tolerance: exact
    depends_on: null
    skip_reason: null
    note: "Optimizes system graph. Requires super-user (meta_write_access). Always available on running TerminusDB."
    assertion: "Exit code 0 AND HTTP 200 AND response contains 'api:success'. Requires admin credentials."

  - block_index: 3
    language: json
    line: 89
    type: fenced
    action: "Display expected success response from optimize endpoint"
    expected_outcome:
      type: skip
      value: "Shows {type: 'api:OptimizeResponse', 'api:status': 'api:success'}."
      tolerance: null
    depends_on: 0
    skip_reason: display_only
    assertion: "No assertion — display only. Expected response format for all optimize calls."

  - block_index: 4
    language: bash
    line: 98
    type: fenced
    action: "CLI: terminusdb optimize for branch and system database"
    expected_outcome:
      type: exit_code
      value: "Exit code 0 — optimization completed"
      tolerance: exact
    depends_on: null
    skip_reason: null
    note: "CLI alternative to HTTP API. Requires terminusdb CLI installed and accessible."
    assertion: "Exit code 0. SKIP unless terminusdb CLI is installed and MyDatabase exists."

  - block_index: 5
    language: bash
    line: 110
    type: fenced
    action: "CLI: terminusdb rollup for targeted layer optimization (single commit)"
    expected_outcome:
      type: exit_code
      value: "Exit code 0 — rollup layer created"
      tolerance: exact
    depends_on: null
    skip_reason: null
    note: "Rollup creates optimization layer for specific commit without full squash. Different from optimize which uses exponential strategy."
    assertion: "Exit code 0. SKIP unless terminusdb CLI is installed and MyDatabase exists."
