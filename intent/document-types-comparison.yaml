# Intent Capture: document-types-comparison
# Page: src/app/docs/document-types-comparison/page.md
# Title: TerminusDB Document Types Compared
# Total blocks: 9
# Annotated by: systems-engineer (technical assertions)
# Date: 2026-05-02
# Status: COMPLETE
# Note: Reference/explanation page comparing regular documents, subdocuments, and @shared documents. All blocks are display-only JSON schema definitions and worked examples. No executable commands. @shared available from v12.0.6.

page: document-types-comparison
total_blocks: 9
auto_filled: 9
needs_manual: 0
auto_fill_rate: 100%

blocks:

  - block_index: 0
    language: json
    line: 50
    type: fenced
    action: "Display schema definition for regular document: Person class with Lexical key"
    expected_outcome:
      type: skip
      value: "Shows Person class with @type: Class, @id: Person, @key: Lexical on 'name', fields: name (xsd:string), email (xsd:string)."
      tolerance: null
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — display only. Schema definition reference for regular documents."

  - block_index: 1
    language: json
    line: 66
    type: fenced
    action: "Display schema definition for subdocument: Address class with @subdocument and Random key"
    expected_outcome:
      type: skip
      value: "Shows Address class with @subdocument: [], @key: Random, fields: street, city, postal_code (all xsd:string)."
      tolerance: null
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — display only. Schema definition reference for subdocuments."

  - block_index: 2
    language: json
    line: 88
    type: fenced
    action: "Display schema definition for shared document: Footnote class with @shared and Lexical key"
    expected_outcome:
      type: skip
      value: "Shows Footnote class with @shared: [], @key: Lexical on 'label', fields: label (xsd:string), content (xsd:string)."
      tolerance: null
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — display only. Schema definition reference for @shared documents (v12.0.6+)."

  - block_index: 3
    language: unknown
    line: 112
    type: fenced
    action: "Display ASCII diagram showing @shared liveness: two Articles referencing one Footnote, cascade on last delete"
    expected_outcome:
      type: skip
      value: "Shows Article A and Article B both pointing to Footnote/1. Delete A → Footnote alive. Delete B → Footnote cascade-deleted."
      tolerance: null
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — display only. Visual explanation of @shared reference counting."

  - block_index: 4
    language: json
    line: 136
    type: fenced
    action: "Display schema for worked example: Footnote (@shared) and Article classes with Set of Footnote references"
    expected_outcome:
      type: skip
      value: "Shows two schema documents: Footnote with @shared/Lexical key, Article with Lexical key and 'notes' as Set of Footnote."
      tolerance: null
    depends_on: null
    skip_reason: display_only
    assertion: "No assertion — display only. Schema setup for lifecycle worked example."

  - block_index: 5
    language: json
    line: 149
    type: fenced
    action: "Display worked example Step 2: create standalone @shared Footnote (no parent references yet)"
    expected_outcome:
      type: skip
      value: "Shows {type: Footnote, label: 'fn1', content: 'See appendix A.'}. Creates Footnote/fn1 independently."
      tolerance: null
    depends_on: 4
    skip_reason: display_only
    assertion: "No assertion — display only. Illustrates standalone @shared creation without triggering cascade check."

  - block_index: 6
    language: json
    line: 157
    type: fenced
    action: "Display worked example Step 3: create two Articles referencing the shared Footnote"
    expected_outcome:
      type: skip
      value: "Shows two Article documents (intro, appendix) both with notes: ['Footnote/fn1']. Establishes 2 references to shared doc."
      tolerance: null
    depends_on: 5
    skip_reason: display_only
    assertion: "No assertion — display only. Establishes multi-reference scenario."

  - block_index: 7
    language: json
    line: 166
    type: fenced
    action: "Display worked example Step 4: delete one parent (Article/intro) — Footnote survives"
    expected_outcome:
      type: skip
      value: "Shows DELETE Article/intro. Footnote/fn1 still alive because Article/appendix still references it."
      tolerance: null
    depends_on: 6
    skip_reason: display_only
    assertion: "No assertion — display only. Demonstrates partial reference removal — @shared survives."

  - block_index: 8
    language: json
    line: 174
    type: fenced
    action: "Display worked example Step 5: delete last parent (Article/appendix) — Footnote cascade-deleted"
    expected_outcome:
      type: skip
      value: "Shows DELETE Article/appendix. Zero references to Footnote/fn1 remain — automatically cascade-deleted at commit time."
      tolerance: null
    depends_on: 7
    skip_reason: display_only
    assertion: "No assertion — display only. Demonstrates cascade deletion when zero references remain."
